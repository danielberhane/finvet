# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# Virtual environment
.venv/
venv/

# Secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets/
private/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Data — re-fetchable from SEC EDGAR, not committed (see scripts/fetch_filings.py)
data/filings/
data/*.db
data/*.sqlite

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Local working notes, not published
docs/EVAL_DATA_POLICY.md
docs/AGENTIC_EVAL_GUIDE.md

# Claude Code worktrees — a nested git repo; never a submodule of this one.
.claude/worktrees/

# Coverage artifact
.coverage

# Agent instructions for other tools; CLAUDE.md is committed deliberately --
# it orients sessions on the hooks and encodes the project's failure history
AGENTS.md
# Mirrors of the hooks/skills for other agent tools; same reason
.agents/
.codex/

# local study notes, never committed
notes/
docs/RAG_AND_AGENTIC_RAG_GUIDE.md
docs/STATE_WRITERS.md
docs/TEST_EDGE_CASES.md
docs/RELEASE_A_DECISIONS.md
docs/VALIDATION_STRATEGY.md
